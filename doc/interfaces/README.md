# Interfaces

This directory contains interface specifications (INT-NNN) for the pico-racer host application.

For GPU register interface specs (INT-010 through INT-014), see `external/pico-gs/doc/interfaces/`.
